import css from 'styled-jsx/css';

import { colors } from '../../tokens';

export default css`
  blockquote {
    background-color: ${colors.yellow};
    color: ${colors.blue};
    font-size: 3rem;
    margin: 0;
    text-align: center;
  }
`;
