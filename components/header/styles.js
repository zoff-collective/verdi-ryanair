import css from 'styled-jsx/css';

import { colors } from '../../tokens';

export default css`
  header {
    background-color: ${colors.yellow};
    color: ${colors.blue};
    margin-bottom: 0;
    margin-top: 0;
    padding: 1rem;
  }

  .name {
    margin-bottom: 0;
    margin-top: 0;
  }
`;
